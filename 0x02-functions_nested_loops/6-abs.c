#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @x: value of function
 *
 * Description: return results
 *
 * Return: interger
 */
int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (x * -1);
	return (0);
}
